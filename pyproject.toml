[project]
name = "gorbe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cat", email = "hesam123409@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
gorbe = "gorbe:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
